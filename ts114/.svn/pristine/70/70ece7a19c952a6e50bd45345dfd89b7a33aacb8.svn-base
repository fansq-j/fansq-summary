package com.len.ts114.service;


import com.len.base.BaseService;
import com.len.ts114.entity.Fserver;
import com.len.util.JsonUtil;

public interface FserverService extends BaseService<Fserver, String> {
    JsonUtil addFserver(Fserver Fserverr);
    JsonUtil checkFserver(Fserver Fserverr);
    JsonUtil updateFserver(Fserver fserver);
    JsonUtil delFserver(Long id);
}
